# M240 - S054 executable public documentation API reference and legacy migration

## Stage

S054 - GSP 0.2 Protocol API And Documentation Consolidation

## Status

Completed.

## Summary

Deliver executable tutorials, curated API reference, precise support tables, redirects, and an
explicitly isolated legacy archive without inventing unavailable APIs.

## Approval

Approved by the project owner's instruction to execute the full breaking consolidation.

## Result

Replaced the repository landing page and public learning path with GSP 0.2 terminology and the sole
`gsp_vispy2` producer import. The first tutorial now includes a checked-in executable source file,
the API reference is curated by producer/protocol role, exact support claims link to generated
profiles, and the site includes migration guidance, screenshot provenance, legacy URL redirects,
repository links, and light/dark presentation. The executable tutorial, redirect outputs, full test
suite (649 passed, 2 skipped), and strict site build pass.
