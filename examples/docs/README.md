# Documentation examples

These scripts are the executable source for public tutorials. Run them from the repository root so
the docs and tested examples stay synchronized.

```bash
uv run python examples/docs/first_scene.py --output /tmp/gsp-first-scene.png
```
